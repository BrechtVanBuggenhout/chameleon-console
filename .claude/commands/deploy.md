Deploy the console to production.

The console deploys automatically via Vercel on every push to `main`. To deploy:

1. Make sure all changes are committed.
2. Push to main:
```bash
git push origin main
```

Vercel will pick it up within ~60 seconds.
